interface:
  display_name: "Fly Dev"
  short_description: "Repo-specific patterns and workflows for fly-dev"
  default_prompt: "Use the fly-dev repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true